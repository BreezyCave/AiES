% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/250221_axQnt_v2.0.01.R
\name{axQnt}
\alias{axQnt}
\title{Axon integrity index quantification}
\usage{
axQnt(imprtImg = TRUE, subBack = 30, expSip = TRUE)
}
\arguments{
\item{imprtImg}{Logical. If TRUE, imports image files (.tiff) for processing. If FALSE, imports precomputed data files (.txt) (default: TRUE).}

\item{subBack}{Numeric. Threshold for subtracting background objects based on size (default: 30 pixels).}

\item{expSip}{Logical. If TRUE, exports single image prediction results as .csv files (default: TRUE).}
}
\value{
The function does not return values directly but generates the following outputs:
\itemize{
\item A summary .csv file containing the calculated Axon Integrity Index (AII) and Degeneration Index (DI) for each file.
\item (Optional) Single image prediction results as .csv files if \code{expSip = TRUE}.
\item (Optional) Processed image data as .txt files if \code{imprtImg = TRUE}.
}
}
\description{
\code{axQnt} quantifies the axonal integrity index (Axon Integrity Index, AII) and degeneration index (Degeneration Index, DI)
from either image files (.tiff) or precomputed data files (.txt). The function uses a pre-trained SVM model to classify
axonal regions and calculates the indices based on the classified areas. It also exports processed data and summary files.
}
\details{
The function processes input data as follows:
\enumerate{
\item Prompts the user to select a pre-trained SVM model file.
\item Depending on \code{imprtImg}, it either processes image files (.tiff) to extract features or directly uses precomputed feature data (.txt).
\item Uses the loaded SVM model to classify axonal regions into "Degenerate" or "Intact".
\item Calculates Axon Integrity Index (AII) and Degeneration Index (DI) based on classified areas:
[
\deqn{AII = \frac{\text{Area of Intact Axons}}{\text{Total Axonal Area}}}
\deqn{DI = \frac{\text{Area of Degenerate Axons}}{\text{Total Axonal Area}}}
]
\item Exports results as summary and optional detailed prediction files.
}
}
\note{
\itemize{
\item The function uses a recursive process to allow continuous processing of multiple directories until the user cancels.
\item Processed image data is exported only if \code{imprtImg = TRUE}.
\item Single image prediction results are exported only if \code{expSip = TRUE}.
}
}
\examples{
\dontrun{
# Process .tiff images to calculate Axon Integrity Index and export single image predictions
axQnt(imprtImg = TRUE, subBack = 30, expSip = TRUE)

# Use precomputed feature data from .txt files without exporting single image predictions
axQnt(imprtImg = FALSE, subBack = 20, expSip = FALSE)

# Adjust background subtraction threshold and process images
axQnt(imprtImg = TRUE, subBack = 50)
}

}
