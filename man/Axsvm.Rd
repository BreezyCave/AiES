% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/250121axSvm008.R
\name{axSvm}
\alias{axSvm}
\title{Create a classifier based on SVM machine learning}
\usage{
axSvm(nCst = 3, nGmm = 0.1, nCrss = 5)
}
\arguments{
\item{nCst}{Numeric. Cost parameter for libsvm (default: 3).}

\item{nGmm}{Numeric. Gamma parameter for libsvm (default: 0.1).}

\item{nCrss}{Integer. Number of folds for K-fold cross-validation (default: 5).}
}
\value{
This function doesn't return values directly, but produces the following outputs:
\itemize{
\item A text file containing the extracted data used for machine learning.
\item An .svm file containing the trained SVM model.
}
}
\description{
\code{axSvm} creates a Support Vector Machine (SVM) classifier for binary classification
of image features, typically used to distinguish between "Degenerate" and "Intact" conditions.
}
\details{
The function performs the following steps:
\enumerate{
\item Prompts the user to select directories containing feature data for "Degenerate" and "Intact" conditions.
\item Reads and combines the feature data from both conditions.
\item Trains an SVM model using the specified parameters.
\item Exports the extracted data for machine learning and the trained SVM model to user-specified locations.
}
}
\note{
\itemize{
\item The function will prompt the user to select directories containing .txt files with feature data.
\item It processes all .txt files in the selected directories.
\item The user will be prompted to specify save locations for the extracted data and SVM model.
\item The extracted data file is saved with the name format: "'current_date'_Extracted_data_for_ML.txt"
\item The SVM model file is saved with the name format: "'current_date'_AxClassifer.svm"
}
}
\section{Feature Selection}{

The function uses the following features for SVM training:
"Group", "m.eccentricity", "s.radius.sd", "h.sva.s2", "h.idm.s1", "h.sen.s1", "m.majoraxis"
}

\examples{
\dontrun{
# Basic usage with default parameters
axSvm()

# Custom SVM parameters
axSvm(nCst = 5, nGmm = 0.05, nCrss = 10)
}

}
